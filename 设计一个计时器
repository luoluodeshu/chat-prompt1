#设计一个计时器

>
>我想要一个HTML+JS制作的计时器，我对计时器的样式要求如```里面的文本
>```
>1、网页背景为黑色，字体颜色为白色
>2、时间显示格式为00:00:00
>3、有三种按钮：开始，暂停，清零
>4、获取当前网页的宽度，建立一个<div>，<div>的宽度等于网页的宽度，并把<时间>和<三种按钮>填在这个<div>里面
>5、要求<时间>的字体大小等于<div>宽度的80%
>```

